# Worker app: long-polling Telegram bot, no public HTTP service.
app = "<your-app>"
primary_region = "iad"

[build]

[env]
  DATA_DIR = "/data"

[mounts]
  source = "data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
